<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - User List</title>
    <link rel="stylesheet" href="public/styles.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #ffffff;
            border-radius: 5px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }
        .textstyle {
    background-color: rgb(230, 195, 195); /* Changing background color */
    font-weight: bold; /* Making font bold */
    border-radius: 20px; /* Making border radius */
    width: auto; /* Making auto-sizable width */
    height: 100px; /* Making auto-sizable height */
    padding: 1px 20px 1px 20px; /* Making space around letters */
    font-size: 18px; /* Changing font size */
}
        h1 {
            color: #000000;
            padding: 20px;
            text-align: center;
            font-size: 35px;
            margin-bottom: 20px;
        }

        ul {
            list-style: none;
            padding: 0;
        }

        li {
            background-color: #dabfbf;
            margin: 10px 0;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        li a {
            text-decoration: none;
            color: #007BFF;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        a.delete-link {
            color: red;
        }

        a.delete-link:hover {
            text-decoration: underline;
            color: darkred;
        }

        a.edit-link {
            color: #007BFF;
        }

        a.edit-link:hover {
            text-decoration: underline;
            color: #0056b3;
        }

        a.add-link {
            font-size: 18px;
            font-weight: bold;
            text-align: center;
            margin-top: 20px;
            text-decoration: none;
            background-color: #0056b3;
            color: #fff;
            height: 40px;
            line-height: 40px;
            border-radius: 3px;
            display: inline-block;
            padding: 0 20px;
        }

        a.add-link:hover {
            background-color: #004699;
        }
    </style>
</head>
<body>
    <H1>ADMIN PANEL</H1>
    <div class="textstyle"><h1>user list</h1></div>
    <div class="container"> <!-- Add a container -->
        <ul>
            <% users.forEach(function(user) { %>
                <li>
                    <%= user.email %>
                    <a href="/edit/<%= user._id %>" class="edit-link">Edit</a>
                    <a href="/delete/<%= user._id %>" class="delete-link">Delete</a>
                </li>
            <% }); %>
        </ul>
        <center><a href="/add" class="add-link">Add User</a></center>
    </div> <!-- Close the container -->
</body>
</html>
